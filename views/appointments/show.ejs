<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Appointment Details</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>A doctor in <%= doctor.name %></h1>
    <h2>patient</h2>
    <% if (doctor.patientid._id === user._id) { %>
        <p>This is your appointment!</p>
    <% } else { %>
        <p>Appointment not available, <%= doctor.patientid.username %> does!</p>
        <p>Appointment Date: <%= appointments.appointmentDate %></p>
        <p>Appointment Time: <%= appointments.appointmentTime %></p>
    <% } %>
</body>
</html>